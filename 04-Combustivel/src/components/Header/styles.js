import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  text: {
    textAlign: 'center',
    color: 'black',
    fontSize: 32,
    marginVertical: 20,
    marginHorizontal: 'auto'
  }
})
